import { StyleSheet } from "react-native"

export const button = StyleSheet.create({
  logout: {
    backgroundColor: "#191970",
    padding: 5,
    borderRadius: 10,
    width: 200,
    fontSize: 20,
    color: "#fff",
  },
})
